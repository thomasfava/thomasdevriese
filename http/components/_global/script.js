(function (document, Drupal) {

  'use strict';

  Drupal.compony = {};

  Drupal.compony.init = {};

  /*
  Drupal.behaviors.compony_MyBehaviour = {
    attach: function (context, settings) {
      if (!Drupal.compony.init) {
        // Execute code only once here:

        // Leave this to make sure this if-structure only runs once.
        Drupal.compony.init = true;
      }

      // Execute code for all DOM updates and intial DOM lad.

    }
  };
  */

})(document, Drupal);
